<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- public/styles.css -->
    <link rel="stylesheet" href="/public/style.css" type="text/css">
    <!-- Bootstrap 4.0 CSS -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
      integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2"
      crossorigin="anonymous"
    />
    <title>Sighting Index: Edit Mode</title>
  </head>
  <body>
    <!-- Start of Navbar -->
    <%- include('navbar') -%>
    <!-- End of Navbar --> 

    <!-- Start of Summary Form (Edit) -->
    <div class="container w-75">
      <form method="POST" action="/sighting/<%=index%>/edit?_method=PUT">
        <!-- <input type="hidden" name="_method" value="put" /> -->
        <h1 class="lead pt-5 pb-4">Editing form #<%= Number(index) + 1 %>.</h1>
        <hr />
        <dl class="row mt-4">
          <dt class="col-sm-3 mb-1">Description</dt>
          <dd class="col-sm-9">
            <textarea id="sighting-edit-desc" class="form-control mb-2" type="text" name="text" id="text" /><%= content.text %></textarea>
          </dd>
          <dt class="col-sm-3">Date & Time</dt>
          <dd class="col-sm-9 mb-3">
            <input
              class="form-control"
              type="text"
              name="date_time"
              id="date_time"
              value="<%= content.date_time %>"
            />
          </dd>
          <dt class="col-sm-3">City</dt>
          <!-- Start of Nesting -->
          <dd class="col-sm-9 mb-0">
            <dl class="row">
              <dd class="col-sm-5">
                <input class="form-control" type="text" name="city" id="city" value="<%= content.city %>"/>
              </dd>
              <dt class="col-sm-2">State</dt>
              <dd class="col-sm-5">
                <input class="form-control" type="text" name="state" id="state" value="<%= content.state %>"/>
              </dd>
            </dl>
          </dd>
          <!-- End of Nesting -->
          <dt class="col-sm-3">Shape</dt>
          <dd class="col-sm-9 mb-3">
            <input
              class="form-control"
              type="text"
              name="shape"
              id="shape"
              value="<%= content.shape %>"
            />
          </dd>
          <dt class="col-sm-3">Duration</dt>
          <dd class="col-sm-9 mb-3">
            <input
              class="form-control"
              type="text"
              name="duration"
              id="duration"
              value="<%= content.duration %>"
            />
          </dd>
          <dt class="col-sm-3">Summary</dt>
          <dd class="col-sm-9 mb-3">
            <textarea class="form-control"  type="text" name="summary" id="summary" /><%=content.summary%></textarea>
          </dd>
        </dl>
        <!-- <div class="row"> -->
          <button type="submit" class="btn btn-info px-4 mt-3 mb-5">Edit Complete</button>
      </form>
      <!-- End of Summary Form (Edit) -->
          <form method="POST" action="/sighting/<%=index%>/delete?_method=DELETE">
            <button type="submit" class="btn btn-danger px-5 mx-4 mt-3 mb-5" name="_method" value="DELETE">Delete</button>
          </form>
      <!-- </div> -->
    </div>

    <!-- Bootstrap JS -->
    <%-include('bootstrap-scripts')-%>
  </body>
</html>
