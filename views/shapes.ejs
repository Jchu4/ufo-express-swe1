<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- public/styles.css -->
  <link rel="stylesheet" href="/public/style.css" type="text/css">
  <!-- Bootstrap 4.0 CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
  <title>Sighting Shapes</title>
</head>
<body>
  <!-- Start of Navbar -->
  <%- include('navbar') -%>
  <!-- End of Navbar --> 

  <div class="container w-75">
    <div class="row">
      <div class="col">
        <h1 class="pt-4">Shapes</h1>
        <p class="font-weight-light text-muted pb-4">Between 2007 and 2020 The Pentagon spent over US$22 million investigating the possible existence of flying saucers through its Advanced Aerospace Threat Identification Program (AATIP), here is a list of some of the witnessed shapes: </p>

        <ul class="list-group">
         <% const shapeCounter = {} %>

         <% for (let i = 0; i < content.length; i ++) { %>
           <% let shape = content[i].shape; %>
          
           <% if (shapeCounter[shape]) { %>
             <% shapeCounter[shape] += 1; %>
           <% } else { %>
             <% shapeCounter[shape] = 1; %>
           <% } %>
        <% } %>

        <!-- Start of forEach() Shape logic occurs here. -->
        <% Object.entries(shapeCounter).forEach(([key, value]) => { %>
          <form method="GET" action="/shapes/<%=key%>">
            <li class="list-group-item d-flex align-items-center">
              <%=key%>
              <span class="badge badge-warning badge-pill text-dark ml-4"><%=value%></span>
              <!-- View More Btn Will Be Enclosed In <form> Tag -->
              <button class="btn btn-outline-info btn-sm ml-auto">
                View All
              </button>
            </li>
          </form>
          <!-- End of forEach() Shape logic occurs here. -->
        <% }) %>

        </ul>
      </div>
    </div>
  </div>
  <!-- Bootstrap JS -->
  <%-include('bootstrap-scripts')-%>
</html>